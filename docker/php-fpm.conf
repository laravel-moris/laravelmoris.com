[global]

; Log errors to container stderr
error_log = /dev/stderr

; Run in foreground (required for containers)
daemonize = no

; PID file location (writable in container)
pid = /tmp/php-fpm.pid

; Include pool configurations (Alpine PHP path)
include = /usr/local/etc/php-fpm.d/*.conf
